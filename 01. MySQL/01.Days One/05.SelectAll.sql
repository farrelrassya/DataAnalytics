/*
This query retrieves all columns from the EmployeeDemographics/EmployeeSalary table.
The purpose of this query is to provide a full view of the demographic data 
of all employees within the organization.

The asterisk (*) is used as a wildcard to select all available columns.

*/
SELECT * 
FROM EmployeeDemographics;

SELECT *
FROM EmployeeSalary
